# toy_chrdev_driver

a toy driver. don't expect much. will share notes later.

- branch `interrupt` allows syscall `open`, `close`, `read`, `write`
- branch `kernel-bypass` allows `mmap` and maps the buffer to userspace
