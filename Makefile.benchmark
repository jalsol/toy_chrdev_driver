# Makefile for benchmark program
CC = g++
CFLAGS = -std=c++20 -Wall -Wextra -O3 -march=native

all: benchmark

benchmark: benchmark.cpp
	$(CC) $(CFLAGS) -o benchmark benchmark.cpp

clean:
	rm -f benchmark
